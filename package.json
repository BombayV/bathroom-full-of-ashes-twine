{
  "name": "the-bathroom-full-of-ashes",
  "description": "A Twine story.",
  "module": "index.ts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun run install-tweego && bun run index.ts",
    "install-tweego": "bun run scripts/install-tweego.ts",
    "watch": "bun run index.ts --watch",
    "dev": "bun run watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "@types/bun": "latest",
    "tailwindcss": "^4.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
